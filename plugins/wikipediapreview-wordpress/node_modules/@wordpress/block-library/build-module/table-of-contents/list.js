import { createElement } from "@wordpress/element";
const ENTRY_CLASS_NAME = 'wp-block-table-of-contents__entry';
export default function TableOfContentsList({
  nestedHeadingList
}) {
  return nestedHeadingList.map((childNode, index) => {
    const {
      anchor,
      content
    } = childNode.heading;
    const entry = anchor ? createElement("a", {
      className: ENTRY_CLASS_NAME,
      href: anchor
    }, content) : createElement("span", {
      className: ENTRY_CLASS_NAME
    }, content);
    return createElement("li", {
      key: index
    }, entry, childNode.children ? createElement("ul", null, createElement(TableOfContentsList, {
      nestedHeadingList: childNode.children
    })) : null);
  });
}
//# sourceMappingURL=list.js.map