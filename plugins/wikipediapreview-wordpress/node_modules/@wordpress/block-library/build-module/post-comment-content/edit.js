import { createElement } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { RawHTML } from '@wordpress/element';
import { useEntityProp } from '@wordpress/core-data';
import { useBlockProps, Warning } from '@wordpress/block-editor';
import { Disabled } from '@wordpress/components';
export default function Edit({
  context: {
    commentId
  }
}) {
  const blockProps = useBlockProps();
  const [content] = useEntityProp('root', 'comment', 'content', commentId);

  if (!(content !== null && content !== void 0 && content.rendered)) {
    return createElement("div", blockProps, createElement(Warning, null, __('Comment has no content.')));
  }

  return createElement("div", blockProps, createElement(Disabled, null, createElement(RawHTML, {
    key: "html"
  }, content.rendered)));
}
//# sourceMappingURL=edit.js.map